Testing reached `/api/workspaces/42/members` and the response used the current
workspace id. However, it is worth noting that the access-control flow appears
to be inconsistent. The report has one concrete endpoint but still uses some
generic transition language.

